{
  "extends": "../tsconfig.base.json",
  "references": [
      { "path": "../data" }
  ],
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist",                                   /* Specify an output folder for all emitted files. */
  }
}
